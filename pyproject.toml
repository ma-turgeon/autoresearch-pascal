[project]
name = "autoresearch"
version = "0.1.0"
description = "Autonomous pretraining research swarm (Pascal/GTX 1080 Ti fork)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "pyarrow",
    "pyyaml",
    "rich",
    "rustbpe",
    "tiktoken",
    "tqdm",
    "torch==2.0.1",
    "huggingface-hub",
]
